# Production dependencies
python-mpd2>=3.0.0
requests>=2.25.0
websockets>=10.0

# Web interface dependencies
Flask>=2.3.0
Flask-CORS>=4.0.0

# Display system dependencies (core - always needed)
Pillow>=9.0.0

# Hardware display dependencies (Raspberry Pi only)
# For SSD1322 OLED display via SPI
luma.oled>=3.8.0
luma.core>=2.4.0
spidev>=3.5
RPi.GPIO>=0.7.1

# GPIO expander for buttons (Raspberry Pi only)
# For MCP23017 I2C GPIO expander with 15 buttons
adafruit-circuitpython-mcp230xx>=2.5.0
adafruit-blinka>=8.0.0

# Bluetooth audio source (Linux/Raspberry Pi only)
# System packages required (install first):
#   sudo apt-get install python3-dbus python3-gi python3-gi-cairo gir1.2-glib-2.0 bluez pulseaudio pulseaudio-module-bluetooth
# Then install Python packages:
dbus-python>=1.2.0

#Use system package for PyGObject
#PyGObject>=3.42.0

# Optional dependencies
# colorama>=0.4.0  # For colored console output on Windows
# click>=8.0.0     # For CLI interfaces

# Development and Windows service dependencies (optional)
# pywin32>=306     # For Windows service functionality (Windows only)
